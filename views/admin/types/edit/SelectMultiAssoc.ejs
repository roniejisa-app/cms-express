<div class="form-group mb-3">
  <label class="d-block mb-1"><%= field.label %></label>
  <div class="grid">
    <% field.data.forEach(option => { %>
    <label>
      <input name="<%= field.name %>" value="<%= option[field.keyValue] %>" <% if(data[field.name].some(item => item[field.keyValue] === option[field.keyValue])){%> checked <% } %> type="checkbox">
      <%= option[field.keyLabel] %>
    </label>
    <% }) %>
    </input>
  </div>
</div>